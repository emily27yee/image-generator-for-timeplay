# Frame Asset

Place your frame PNG here as `frame.png`.

Requirements:
- PNG format with transparency
- The transparent area is where the user's photo will show through
- Any resolution works — the canvas will match the frame's dimensions

Replace the included sample `frame.png` with your own frame design.
